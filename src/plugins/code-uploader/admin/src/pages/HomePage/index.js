/*
 *
 * HomePage
 *
 */

import React, { memo } from 'react';
// import PropTypes from 'prop-types';
import pluginId from '../../pluginId';
import CodeUploder from '../../components/CodeUploder'

const HomePage = () => {
  return <CodeUploder />;

};

export default memo(HomePage);
